# ==============================================
# TUNISIAN TOP FREELANCERS - BACKEND CONFIG
# ==============================================
# Copy this file to .env and fill in your values

# Environment
NODE_ENV=development
PORT=5000

# MongoDB Atlas Database (REQUIRED)
# 1. Create free account: https://www.mongodb.com/cloud/atlas/register
# 2. Create M0 cluster (free tier)
# 3. Create database user and whitelist IP
# 4. Get connection string and replace <username>, <password>, <cluster>, <database>
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# JWT Authentication (REQUIRED)
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -hex 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Stripe Payment Gateway (REQUIRED)
# Get your keys from: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Google Gemini AI (OPTIONAL - for AI job recommendations)
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend URL (REQUIRED for CORS)
CLIENT_URL=http://localhost:3000
